<?xml version="1.0" encoding="UTF-8"?><odoo>
    <data noupdate="0">
        <!-- <record model="ir.actions.server" id="access_izi">
      <field name="name">Access IZI App</field>
      <field name="model_id" ref="juragan_webhook.model_webhook_server"/>
      <field name="state">code</field>
      <field name="code">
server = model.search([], limit=1)
if server:
    module_obj = env['ir.module.module']
    action = server.remote()

    module_web_enterprise = module_obj.search([('name', '=', 'web_enterprise')])
    if module_web_enterprise and module_web_enterprise.state == 'installed':
        action['target'] = 'self'
else:
    raise Warning("Please add integration first!")
      </field>
    </record> -->
    </data>
</odoo>
